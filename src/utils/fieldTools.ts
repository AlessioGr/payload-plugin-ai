import { Field } from "payload/types";

/* export function isFieldNamed(field: Field): boolean {
  return (
    field.type !== "tabs" &&
    field.type !== "collapsible" &&
    field.type !== "row" &&
    field.type !== "ui"
  );
}
 */
